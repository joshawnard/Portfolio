<%= image_tag @project.main_image unless @project.thumb_image.nil? %>
<h1><%= @project.title %></h1>
<h2><%= @project.subtitle %></h2>
<p><%= @project.body %></p>

<h3>Technologies Used</h3>
<%= @project.technologies.each do |t| %>
  <p><%= t.name %></p>
<% end %>
